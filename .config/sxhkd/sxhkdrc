# .config/sxhkd/sxhkdrc by Miika Nissi
# https://github.com/miikanissi
# https://miikanissi.com

### PROGRAM LAUNCHERS ###
# launch programs (h,j,k,l,d,q,f,t,n,p,space,greater,less taken by bspc and cmus)
super + {Return, w, e, g, m, c, b, s}
    {alacritty, firefox, geary, emacs, urxvt -name ncmpcpp -e ncmpcpp, pcmanfm, urxvt -name newsboat -e newsboat, signal-desktop --use-tray-icon}

# print screen script with maim
super + Print
    ~/.local/bin/screenshot.sh

# print screen with flameshot
super + shift + Print
    flameshot gui

# launch rofi
super + d
    rofi -show run

# launch rofi ssh
super + alt + d
    rofi -show ssh

# launch rofimoji
super + shift + d
    rofimoji -c -s light

# launch rofi window switcher
super + Tab
    rofi -show window

# launch rofi kill script
super + ctrl + alt + d
    .local/bin/rofi_killprocess.sh

# launch passmenu
super + ctrl + d
    .local/bin/passmenu.sh

# launch passmenu_otp
super + ctrl + shift + d
    .local/bin/passmenu_otp.sh

### MPD HOTKEYS ###
# pause mpd
super + space
    mpc toggle

# adjust mpd volume
super + {greater, less}
    mpc volume {+, -}2

# change song
super + {n, p}
    mpc {next, prev}


### BSPC HOTKEYS ###
# restart everything
super + q
    bspc wm -r

# exit bspwm
super + shift + q
    bspc quit

# close current window
super + shift + c
    bspc node -c

# swap the current node and the biggest node
super + shift + Return
    bspc node -s biggest

# state of the window
super + {t,shift + t,shift + f,f}
    bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

#rotate the tree from current nodes parent
super + r
    bspc node @parent --rotate 90

# focus the node in the given direction, shift swaps nodes
super + {_,shift +}{Left,Down,Up,Right}
    bspc node -{f,s} {west,south,north,east}

super + {_,shift +}{h,j,k,l}
    bspc node -{f,s} {west,south,north,east}

# focus or send to the given desktop
super + {_,shift +}{1-8}
    bspc {desktop -f,node -d} '^{1-8}' --follow

# focus or send to desktop up, down
super + {_,shift +}{Next, Prior}
    bspc {desktop -f,node -d} {next, prev} --follow

# expand a window by moving one of its side outward
super + alt + {Left,Down,Up,Right}
    bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

super + alt + {h,j,k,l}
    bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
super + ctrl + {Left,Down,Up,Right}
    bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

super + ctrl + {h,j,k,l}
    bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}
